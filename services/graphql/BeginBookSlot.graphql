mutation BeginBookSlot($slot: Date!, $medical_patient_id: ID!, $medical_provider_id: ID!) {
  begin_book_slot(slot: $slot, medical_patient_id: $medical_patient_id,  medical_provider_id: $medical_provider_id) {
    id
    start_time
    provider_id
    patient_id
    expires_at
    status
  }
}
